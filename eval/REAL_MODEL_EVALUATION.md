# MiniCPM-o 4.5 真实模型评测记录

## 2026-08-15 30 题端到端评测

- 端点：`config/minicpm.json` 配置的远程 vLLM-Omni 服务
- 题集：`eval/questions.json`
- 运行方式：前 3 题并发 1，后 27 题并发 2；每题均执行 hint 与 explain 两阶段
- 原始报告：本机 `eval/results/real-model-20260815-201853.json`、`eval/results/real-model-20260815-202603.json`

| 指标 | 结果 |
| --- | ---: |
| 请求错误 | 0 / 30 |
| 提示阶段不泄露最终答案 | 30 / 30 |
| 保守答案等价判定通过 | 24 / 30 |
| 人工复核后语义正确 | 30 / 30 |
| Hint 平均延迟 | 3.97 s |
| 完整解析平均延迟 | 22.41 s |
| 完整解析 p50 / p95 | 17.60 s / 77.50 s |
| 首事件 p50 / p95 | 0.88 s / 16.97 s |

## 多模态探测

| 能力 | 结果 | 延迟 |
| --- | --- | ---: |
| 文本数学问答 | `2x + 5 = 17` 答对 | 16.93 s |
| 图片识题 | 识别出 `2x + 5 = 17` | 2.70 s |
| TTS | 返回 24 kHz WAV | 11.66 s / 14.35 s |

## 保守判定失败项复核

以下 6 项初始被判错，原因均为答案抽取规则过严；人工复核均为正确：

1. `alg-09`：模型输出 `\(x^2 - 9 = (x + 3)(x - 3)\)`，包含 LaTeX 和完整等式。
2. `alg-10`：模型输出 `$(x + 3)^2$`，包含 LaTeX 定界符。
3. `func-01`：模型输出“点 A 的坐标是 `(3/2, 0)`”，包含自然语言前缀。
4. `func-04`：模型输出完整肯定句，而非单字“是”。
5. `geo-07`：模型输出 `$7\sqrt{2}$`，语义正确。
6. `coord-02`：模型输出 `$\sqrt{17}$`，语义正确。

评测脚本已更新答案规范化逻辑，后续报告会自动剥离常见 LaTeX、等式右侧和坐标前缀。

## 结论

- 真实模型文本讲解、图片识题和 TTS 均可用。
- 教学护栏稳定：30/30 默认提示未泄露最终答案。
- 语义答案正确率 30/30；原始保守判定 24/30。
- 首事件 p50 约 0.9 秒，可支撑流式体验；个别完整解析请求接近 97 秒，演示前建议预热并准备备用题。
